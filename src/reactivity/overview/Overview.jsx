import React, { useEffect } from "react";
import Step from "../impress/step";

const Overview = () => (
  <Step id="overview" x={0} y={0} scale={30} style={{ pointerEvents: "none" }}>
    >
  </Step>
);

export default Overview;
